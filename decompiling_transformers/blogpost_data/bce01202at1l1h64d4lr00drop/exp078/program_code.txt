1. s1 = select(q=token, k=token, op=\circled{a})	# layer 0 head 0
2. s2 = select(q=token, k=pos, op=(k is first),
		special_op=(uniform selection))	# layer 0 head 0
3. a1 = aggregate(s=s1+s2, v=token) 	  # layer 0 head 0
4. new_a1 = element_wise_op(a1) 	  # layer 0 mlp
5. logits1 = project(inp=new_a1, op=(inp==out))
6. prediction = sigmoid(logits1)