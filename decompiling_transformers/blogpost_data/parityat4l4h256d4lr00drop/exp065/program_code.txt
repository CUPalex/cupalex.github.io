1. s1 = select(q=token, k=token, op=(uniform selection),
		special_op=(k==BOS))	# layer 0 head 2
2. a1 = aggregate(s=s1, v=token) 	  # layer 0 head 2
3. new_a1 = element_wise_op(a1) 	  # layer 0 mlp
4. new_new_a1 = element_wise_op(new_a1) 	  # layer 2 mlp
5. new_new_new_a1 = element_wise_op(new_new_a1) 	  # layer 3 mlp
6. logits1 = project(inp=new_new_new_a1, op=(out==EOS))
7. prediction = softmax(logits1)