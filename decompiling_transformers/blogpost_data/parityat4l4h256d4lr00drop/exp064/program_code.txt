1. a1 = aggregate(s=[], v=pos) 	  # layer 0 head 2
2. new_a1 = element_wise_op(a1) 	  # layer 0 mlp
3. new_new_a1 = element_wise_op(new_a1) 	  # layer 2 mlp
4. logits1 = project(inp=new_new_a1, op=(out==EOS))
5. prediction = softmax(logits1)