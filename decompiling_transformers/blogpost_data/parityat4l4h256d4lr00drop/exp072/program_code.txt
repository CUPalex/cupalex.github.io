1. s1 = select(q=pos, k=token, op=(k==BOS))	# layer 0 head 2
2. a1 = aggregate(s=s1, v=pos) 	  # layer 0 head 2
3. new_a1 = element_wise_op(a1) 	  # layer 0 mlp
4. new_new_a1 = element_wise_op(new_a1) 	  # layer 3 mlp
5. logits1 = project(inp=new_a1, op=(inp==out))
6. logits2 = project(inp=new_new_a1, op=(inp==out))
7. prediction = softmax(logits1+
			logits2)