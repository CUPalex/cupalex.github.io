1. s1 = select(k=token, op=(k==EOS))
2. a1 = aggregate(s=s1, v=token) 	  # layer 0 head 2
3. new_a1 = element_wise_op(a1) 	  # layer 0 mlp
4. logits1 = project(inp=new_a1, op=(inp==out))
5. prediction = softmax(logits1)