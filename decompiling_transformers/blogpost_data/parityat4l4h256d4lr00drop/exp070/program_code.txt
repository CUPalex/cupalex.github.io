1. a1 = aggregate(s=[], v=token) 	  # layer 0 head 2
2. a2 = aggregate(s=[], v=pos) 	  # layer 0 head 2
3. new_a1 = element_wise_op(a1) 	  # layer 0 mlp
4. new_a2 = element_wise_op(a2) 	  # layer 0 mlp
5. new_new_a1 = element_wise_op(new_a1) 	  # layer 3 mlp
6. new_new_a2 = element_wise_op(new_a2) 	  # layer 3 mlp
7. logits1 = project(inp=new_new_a1, op=(out==EOS))
8. logits2 = project(inp=new_new_a2, op=(out==EOS))
9. prediction = softmax(logits1+
			logits2)